engines:
    csslint:
        enabled: true
    phpmd:
        enabled: true
        rulesets: ["codesize", "design", "naming", "cleancode", "unusedcode"]
ratings:
    paths:
        - "**.css"
        - "**.php"
